(function(ns) {

  'use strict';

  ns.config = {};

  // default config
  ns.config.themeCSSClass = 'default';
  ns.config.highlightCSSClass = 'highlight';
  ns.config.defaultFontSize = '12px';

}(window.stepViz));
